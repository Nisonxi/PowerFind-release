# PowerFind

PowerFind release channel — hosts the auto-update manifest (`version.json`)
and release downloads (installer + portable zip) for the
[PowerFind](https://powerfind.app) desktop search app.

This repo is public (not linked from search/discovery); it only serves the
app's self-updater and the website's download buttons.
